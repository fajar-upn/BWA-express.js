<!-- Header -->
<%- include('../../../partials/header.ejs') %>
    <!-- End of Header -->
    <!-- Page Wrapper -->
    <div id="wrapper">

        <!-- Sidebar -->
        <%- include('../../../partials/sidebar.ejs') %>
            <!-- End of Sidebar -->

            <!-- Content Wrapper -->
            <div id="content-wrapper" class="d-flex flex-column">

                <!-- Main Content -->
                <div id="content">

                    <!-- Topbar -->
                    <%- include('../../../partials/navbar.ejs') %>
                        <!-- End of Topbar -->

                        <!-- Begin Page Content -->
                        <div class="container-fluid">

                            <!-- flash message -->
                            <%- include('../../../partials/message.ejs') %>
                                <!-- end of flash message -->

                                <!-- Page Heading -->
                                <h1 class="h3 mb-4 text-gray-800">Detail Item</h1>
                                <!-- End of Heading -->

                                <!-- navigation in show_table_item.ejs -->
                                <ul class="nav nav-tabs" id="myTab" role="tablist">
                                    <li class="nav-item" role="presentation">
                                        <a class="nav-link active" id="home-tab" data-toggle="tab" href="#show-feature"
                                            role="tab" aria-controls="home" aria-selected="true">Show Feature</a>
                                    </li>
                                    <li class="nav-item" role="presentation">
                                        <a class="nav-link" id="profile-tab" data-toggle="tab" href="#show-activity"
                                            role="tab" aria-controls="profile" aria-selected="false">Show Activity</a>
                                    </li>
                                </ul>
                                <div class="tab-content" id="myTabContent">
                                    <%- include('show_feature.ejs') %>
                                        <%- include('show_activity.ejs') %>
                                </div>
                                <!-- end of navigation -->
                        </div>
                        <!-- /.container-fluid -->

                </div>
                <!-- End of Main Content -->

                <!-- Footer -->
                <%- include('../../../partials/footer.ejs') %>
                    <!-- End of Footer -->

            </div>
            <!-- End of Content Wrapper -->

    </div>
    <!-- End of Page Wrapper -->

    <!-- Scroll to Top Button-->
    <a class="scroll-to-top rounded" href="#page-top">
        <i class="fas fa-angle-up"></i>
    </a>

    <!-- Logout Modal-->
    <!-- <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
                    <a class="btn btn-primary" href="login.html">Logout</a>
                </div>
            </div>
        </div>
    </div> -->

    <!-- javascript library -->
    <%- include('../../../partials/js.ejs') %>
        <!-- modal feature and modal activity -->
        <%- include('show_modal_feature.ejs') %>
            <%- include('show_modal_activity.ejs') %>

                <script>
                    // #data-table-activity must above #data-table-feature, because javascript will conflict
                    /**
                     * need this code for show table in show_activity.ejs
                     * this code needed because we use id="dataTable" in the show feature
                     */
                    $(document).ready(function () {
                        $('#data-table-activity').DataTable()
                    })

                    /**
                     * JQuery find me id='dataTable', 
                     * when data table clicked find me class="button-update".
                     * lastly get data from show_modal_feature.ejs.
                     * 
                     * data('id') will find data-id="" in the show_feature.ejs
                     */
                    $('#data-table-activity').on('click', '.button-update', function () {
                        let id = $(this).data('id')
                        let name = $(this).data('name')
                        let type = $(this).data('type')

                        $('#edit-modal-activity').modal('show')
                        $('.id').val(id)
                        $('.name').val(name)
                        $('.type').val(type)
                    })

                    /**
                     * JQuery find me id='dataTable', 
                     * when data table clicked find me class="button-update".
                     * lastly get data from show_modal_feature.ejs.
                     * 
                     * data('id') will find data-id="" in the show_feature.ejs
                     */
                    $('#data-table-feature').on('click', '.button-update', function () {
                        let id = $(this).data('id')
                        let name = $(this).data('name')
                        let qty = $(this).data('qty')

                        $('#edit-modal-feature').modal('show')
                        $('.id').val(id)
                        $('.name').val(name)
                        $('.qty').val(qty)
                    })



                    // Ckeditor
                    ClassicEditor
                        .create(document.querySelector('#about'))
                        // .then(editor => {
                        //     console.log(editor);
                        // })
                        .catch(error => {
                            console.error(error);
                        });
                </script>

                <!-- Logout Modal-->
                <%- include('../../../partials/logout_modal.ejs') %>
                    <!-- end of modal -->